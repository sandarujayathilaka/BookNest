<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <solid android:color="@android:color/white" /> <!-- Background color -->

    <!-- Set the corner radius for rounded corners (optional) -->
    <corners android:radius="8dp" />

    <!-- Define the border with stroke properties -->
    <stroke
        android:width="2dp"
        android:color="@android:color/darker_gray" /> <!-- Border color and width -->
</shape>